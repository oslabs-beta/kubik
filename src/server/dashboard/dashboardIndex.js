const kubernetesViewsNodes = require('../dashboard/kubernetesViewsNodes.json');
const nodeExporter = require('../dashboard/nodeExporter.json');

const dashboards = {
  kubernetesViewsNodes,
  nodeExporter,
};

module.exports = dashboards;
