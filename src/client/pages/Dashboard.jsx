import React from 'react';
import Grid from '@mui/material/Grid';
import Panel from '../components/Panel/Panel';

const Dashboard = () => {
  // Set states for dashboard to display
  // const [kubePromDashboard, setKubePromDashboard] = useState([]);
  // const [kubeStateDashboard, setKubeStateDashboard] = useState([]);

  // Declare function to handle switching dashboards
  // const handleDashboardChange = () => {};

  return (
    <Grid item xs={8}>
      This is dashboard
    </Grid>
  );
};

export default Dashboard;
